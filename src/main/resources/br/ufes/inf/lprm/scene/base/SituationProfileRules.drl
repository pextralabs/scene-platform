package br.ufes.inf.lprm.scene.base;

global SituationProfileManager SPM;

rule "SetupSituationProfileManager"
	when
		not(exists (SituationProfileManager()))
	then
		SituationHelper.SetupSituationProfileManager(drools);
end

